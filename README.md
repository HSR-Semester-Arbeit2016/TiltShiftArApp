# TiltShiftArApp
First AR prototype with tilt shift filter

In order to get this project working in Unity, after cloning the proyect and opening it in Unity you will have to search for the MainScene in the Assets Folder.
Remove the default ```Untitled``` Scene in Unity and Drag & Drop the MainScene to the Scene Hierarchy Tab in Unity
